# Full stack: portfolio + Handoff API in one container
# Run: docker-compose up
# Open: http://localhost:3000/projects/handoff/
# Copy .env.example to .env and add your API keys for full functionality
services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
