---
phase: 02-claude-code-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .mcp.json
autonomous: true
requirements:
  - CLAUDE-01
  - CLAUDE-02

must_haves:
  truths:
    - "A fresh Claude session can understand the monorepo layout, api/_lib/ patterns, and test locations without reading individual source files"
    - "CLAUDE.md is under 200 lines and uses behavioral rules, not just structural descriptions"
    - "CLAUDE.md documents the oauth.js CORS exception explicitly"
    - ".mcp.json is committed to the repo and contains both Vercel and GitHub MCP entries using HTTP transport with OAuth"
    - "No tokens or secrets appear in .mcp.json"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project context for every new Claude session"
      max_lines: 200
      contains:
        - "api/_lib/"
        - "cors.js"
        - "mongodb.js"
        - "bug report"
        - "REQUIREMENTS.md"
        - "ROADMAP.md"
    - path: ".mcp.json"
      provides: "MCP server configuration for Vercel and GitHub"
      contains: "mcpServers"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "direct link"
    - from: "CLAUDE.md"
      to: ".planning/ROADMAP.md"
      via: "direct link"
    - from: ".mcp.json"
      to: "https://mcp.vercel.com"
      via: "HTTP transport entry"
    - from: ".mcp.json"
      to: "https://api.githubcopilot.com/mcp/"
      via: "HTTP transport entry"
---

<objective>
Expand CLAUDE.md from its current 8-line bug-report-only stub into a full project context document, and create .mcp.json to enable Vercel and GitHub MCP server integrations.

Purpose: A fresh Claude session must be able to orient itself to the post-refactor codebase architecture — the api/_lib/ shared utility patterns, test locations, env vars, and behavioral rules — without reading individual source files. MCP integration gives Claude direct access to Vercel deployments and GitHub issues without leaving the editor.

Output: An expanded CLAUDE.md (under 200 lines, behavioral rules + structure overview + progressive disclosure links) and a .mcp.json with HTTP OAuth entries for Vercel and GitHub.
</objective>

<execution_context>
@C:/Users/adamc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adamc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Current CLAUDE.md (8 lines — expand, do not replace the bug report section) -->
From CLAUDE.md:
```markdown
# adamcamilleri.github.io — Project Context

## Bug Reports
When a bug is reported, do not start by trying to fix it. Instead:
1. Write a test that reproduces the bug first
2. Have subagents attempt the fix
3. Prove the fix works via a passing test
```

<!-- api/_lib/ pattern (from Phase 1 — mandatory for all new API endpoints) -->
From api/_lib/cors.js (created in Phase 1):
- Exports: `setCorsHeaders(req, res)` — call at top of every handler
- Exception: api/oauth.js has its own ALLOWED_ORIGINS (deliberately restricted 4-origin list — do NOT migrate without reviewing the OAuth flow)

From api/_lib/mongodb.js (created in Phase 1):
- Exports: `{ getDb }` — returns a ping-guarded MongoClient
- Pattern: require inside handler body (not at module top)

<!-- API handler template -->
From api/chat.js (canonical example):
```js
module.exports = async function handler(req, res) {
  const { setCorsHeaders } = require('./_lib/cors.js');
  const { getDb } = require('./_lib/mongodb.js');
  setCorsHeaders(req, res);
  if (req.method === 'OPTIONS') return res.status(204).end();
  // method check, validation, business logic...
};
```

<!-- Test locations -->
- Root Jest: `__tests__/*.test.js` — run with `npm test`
- TaskMaster Jest: `projects/taskmaster/` — run with `cd projects/taskmaster && npm test`
- Cypress E2E: `cypress/e2e/` — run with `npm run test:e2e`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Expand the existing CLAUDE.md (currently 8 lines). Retain the Bug Reports section verbatim. Add the following sections in order, keeping the total file under 200 lines:

**Section order:**
1. Top-level header + 2-line purpose line (what this repo is + pointer to STATE.md)
2. Monorepo Layout — compact directory tree (repo root = portfolio shell, api/ = serverless functions, projects/ = mini-apps, api/_lib/ = shared utilities NOT endpoints, .planning/ = planning docs)
3. api/_lib/ Pattern — behavioral rule: "When adding an API endpoint, ALWAYS require cors.js from api/_lib/ at the top of the handler. Exception: api/oauth.js has its own CORS allowlist (deliberately restricted — do not change without reviewing the OAuth flow)." Include the 5-line canonical handler pattern using setCorsHeaders + getDb.
4. How to Run Tests — three commands: `npm test` (Jest root), `cd projects/taskmaster && npm test` (TaskMaster), `npm run test:e2e` (Cypress)
5. Key Env Vars — list: GROQ_API_KEY (required), MONGODB_URI (design save/load), VERCEL_TOKEN (deploy button), VERCEL_OAUTH_CLIENT_ID + VERCEL_OAUTH_CLIENT_SECRET (user OAuth). Note: see .env.example for full list.
6. Adding Code — two mini checklists:
   - New API endpoint: create api/&lt;name&gt;.js, require cors.js first, register in server.js
   - New mini-app project: create projects/&lt;name&gt;/ with index.html + script.js + style.css
7. Bug Reports — (keep existing section — place it here verbatim)
8. Security Rules — four inline rules as a bulleted list:
   - No hardcoded secrets — always use process.env.*
   - All API request bodies must be validated (type, size, required fields)
   - XSS prevention — use DOM APIs (textContent, createElement), never innerHTML with user-supplied content
   - Auth checks must run before any protected operation
9. Deep Context — links to: .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md (current phase + progress), .planning/codebase/ (STRUCTURE.md, CONVENTIONS.md, INTEGRATIONS.md, TESTING.md)

**Tone and style:** Follow the affaan-m/everything-claude-code style — concise section headers, code blocks for commands and patterns, behavioral rules ("always do X") not just facts ("X exists"). Every section should answer "what should I DO here?" not just "what exists here?"

**Hard constraints:**
- File must stay under 200 lines (including blank lines and headers)
- Bug Reports section must be preserved verbatim (it is already used by Claude's memory system)
- oauth.js CORS exception must be explicitly noted under api/_lib/ Pattern
- Do NOT add hooks, /deploy command, /test command, or MongoDB MCP — all deferred per CONTEXT.md
  </action>
  <verify>
    <automated>wc -l CLAUDE.md && grep -c "cors.js" CLAUDE.md && grep -c "REQUIREMENTS.md" CLAUDE.md && grep -c "oauth.js" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md exists, line count is under 200, contains cors.js reference, oauth.js exception note, link to REQUIREMENTS.md, and all 9 required sections</done>
</task>

<task type="auto">
  <name>Task 2: Create .mcp.json</name>
  <files>.mcp.json</files>
  <action>
Create `.mcp.json` at the repo root with the following exact content:

```json
{
  "mcpServers": {
    "vercel": {
      "type": "http",
      "url": "https://mcp.vercel.com"
    },
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/"
    }
  }
}
```

**Key decisions (from research):**
- Both servers use HTTP transport with OAuth — NO tokens in the file
- VERCEL_TOKEN is used by api/deploy.js for programmatic deployment (separate concern) — it does NOT go in .mcp.json
- The Vercel MCP server URL is `https://mcp.vercel.com` (official endpoint as of 2025)
- The GitHub MCP server URL is `https://api.githubcopilot.com/mcp/` (official HTTP remote, replaces Docker approach)
- Do NOT use Docker/stdio config for GitHub — that is the old approach

**After creating the file**, verify it parses as valid JSON.
  </action>
  <verify>
    <automated>node -e "const c = require('./.mcp.json'); console.log('valid:', Object.keys(c.mcpServers).sort().join(','))"</automated>
  </verify>
  <done>.mcp.json exists at repo root, parses as valid JSON, contains mcpServers with "vercel" and "github" entries, no tokens or secrets in the file</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Line count: `wc -l CLAUDE.md` — must be under 200
2. Required sections present: `grep -n "api/_lib\|cors.js\|mongodb.js\|Bug Report\|Security Rules\|Deep Context" CLAUDE.md`
3. oauth.js exception documented: `grep "oauth" CLAUDE.md`
4. .mcp.json validity: `node -e "require('./.mcp.json')" && echo ok`
5. Both MCP servers present: `node -e "const c=require('./.mcp.json'); console.log(c.mcpServers.vercel.url, c.mcpServers.github.url)"`
6. No secrets in .mcp.json: `grep -i "token\|secret\|key" .mcp.json` — must return nothing
</verification>

<success_criteria>
- CLAUDE.md is under 200 lines, contains all 9 required sections, uses behavioral rules not just descriptions, documents the oauth.js CORS exception
- .mcp.json is at repo root, parses as valid JSON, has HTTP+OAuth entries for both Vercel and GitHub, contains no tokens
- Both files are committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-code-tooling/02-01-SUMMARY.md`
</output>