---
phase: 04-per-project-polish-redesigns
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/connect-four/index.html
  - projects/connect-four/style.css
  - projects/connect-four/script.js
autonomous: true
requirements:
  - REDESIGN-04
  - SIG-03

must_haves:
  truths:
    - "Board uses light/clean Apple-esque theme with white background and soft shadows"
    - "Discs drop with a physics bounce animation (fall, bounce once, settle)"
    - "Winning four discs glow and a green line draws through them"
    - "Non-winning discs dim slightly when a win is detected"
    - "Hovering over a column shows a subtle background highlight on that column"
    - "Red discs are #dc2626 and gold discs are #ca8a04"
    - "Dark mode override is removed -- light theme only"
  artifacts:
    - path: "projects/connect-four/style.css"
      provides: "Apple-esque theme, bounce animation, glow effects, column hover"
      contains: "disc-drop"
    - path: "projects/connect-four/script.js"
      provides: "GameUI with bounce drop, glow win animation, column hover highlight"
      contains: "disc-drop"
    - path: "projects/connect-four/index.html"
      provides: "SVG overlay for winning line, updated color scheme"
      contains: "win-line-overlay"
  key_links:
    - from: "projects/connect-four/script.js"
      to: "projects/connect-four/style.css"
      via: "GameUI adds .dropping class for animation, .glow for win highlight"
      pattern: "classList.add.*dropping|classList.add.*glow"
    - from: "projects/connect-four/script.js"
      to: "SVG win-line-overlay"
      via: "GameUI calculates winning line coordinates and animates SVG line"
      pattern: "win-line"
---

<objective>
Redesign Connect Four with a polished Apple-esque theme, physics bounce disc drops, and glowing winning line.

Purpose: Transform Connect Four from functional to visually impressive with satisfying animations. This fulfills both REDESIGN-04 (visual overhaul) and SIG-03 (memorable animation moment -- the bounce drop + glow win combo).
Output: Fully redesigned Connect Four in projects/connect-four/ (3 files modified).
</objective>

<execution_context>
@C:/Users/adamc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adamc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-project-polish-redesigns/04-CONTEXT.md
@.planning/phases/04-per-project-polish-redesigns/04-RESEARCH.md

@projects/connect-four/index.html
@projects/connect-four/script.js
@projects/connect-four/style.css

<interfaces>
GameState class (lines 2-124): Pure game logic, no DOM. Methods: makeMove(col), checkWin(), reset(), undo(). Properties: board[][], currentPlayer, rows=6, cols=7, winner, winningCells[]. DO NOT MODIFY.

GameUI class (lines 126-380): All DOM interaction. Methods:
- initializeBoard(): creates .slot divs in .board-grid
- setupEventListeners(): click on slot -> handleMove(col), mouseover -> showDropPreview(col), mouseout -> clear preview
- handleMove(col): calls gameState.makeMove, updateBoard, checks win/draw
- updateBoard(): reads gameState.board, sets slot classes (red/yellow)
- showDropPreview(col): adds .preview class to target slot
- showWinAnimation(result): highlights winning cells
- showModal(msg)/hideModal(): game over modal
- updateStatus(): turn indicator
- updateScores(): score display
- handleUndo(): undo last move

DOM elements used:
- .board-grid: contains all .slot divs (7 cols x 6 rows)
- .slot[data-row][data-col]: individual cells
- .slot disc rendering: currently creates disc div inside slot
- #undo, #reset, #playAgain, #closeModal buttons
- .status: turn text
- .scores .red-score, .scores .yellow-score: score displays
- .game-modal, .modal-content: game over overlay
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign HTML structure and CSS to Apple-esque theme with animations</name>
  <files>projects/connect-four/index.html, projects/connect-four/style.css</files>
  <action>
In index.html:
- Update the board container to include an SVG overlay for the winning line:
  After the .board-grid div, add an SVG element: svg.win-line-overlay with style="position:absolute;top:0;left:0;width:100%;height:100%;pointer-events:none;". Inside: line.win-line with default x1/y1/x2/y2="0", stroke="#16a34a", stroke-width="4", stroke-linecap="round", stroke-dasharray="500", stroke-dashoffset="500".
- The .board-grid parent container needs position:relative for the SVG overlay to position correctly.
- Keep all existing DOM structure (board-grid, slots are created by JS, modal, buttons, status, scores). Only ADD the SVG overlay.
- Update any inline styles or color references if present.

REWRITE projects/connect-four/style.css entirely:

Color palette:
- --bg: #f8fafc (near-white background)
- --board-bg: #e2e8f0 (light gray board)
- --grid-line: #cbd5e1 (soft grid lines)
- --red: #dc2626 (red discs)
- --gold: #ca8a04 (gold/yellow discs)
- --win-glow: #16a34a (green winning highlight)
- --shadow: rgba(0,0,0,0.08)
- --text: #1e293b (dark slate text)
- --text-muted: #64748b

Body/page:
- background: var(--bg)
- font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
- color: var(--text)
- display flex, justify-content center, align-items center, min-height 100vh, padding 20px

Game container:
- background white
- border-radius 20px
- padding 32px
- box-shadow: 0 4px 24px var(--shadow), 0 1px 3px rgba(0,0,0,0.04)
- max-width: 480px, width: 100%

Board container (parent of .board-grid):
- position: relative (for SVG overlay)
- background: var(--board-bg)
- border-radius: 16px
- padding: 12px
- box-shadow: inset 0 2px 8px rgba(0,0,0,0.06)

Board grid (.board-grid):
- display: grid
- grid-template-columns: repeat(7, 1fr)
- gap: 6px

Slots (.slot):
- aspect-ratio: 1
- border-radius: 50%
- background: var(--bg) (empty slot = white circle on gray board)
- border: 2px solid var(--grid-line)
- transition: background 0.15s, border-color 0.15s
- position: relative, overflow: hidden

Discs inside slots:
- .slot .disc: width 100%, height 100%, border-radius 50%, position absolute, top 0, left 0
- .slot .disc.red: background radial-gradient(circle at 35% 35%, #ef4444, var(--red)). Subtle 3D effect with gradient.
- .slot .disc.yellow: background radial-gradient(circle at 35% 35%, #eab308, var(--gold))
- .slot .disc.dropping: animation disc-drop 400ms ease-out forwards

Disc drop animation (INSIDE @media (prefers-reduced-motion: no-preference)):
- @keyframes disc-drop: 0% translateY(-400%), 50% translateY(0), 65% translateY(-15%), 80% translateY(0), 90% translateY(-5%), 100% translateY(0). This creates the physics bounce.

Column hover:
- .board-grid has data-hover-col attribute set by JS
- .slot[data-col="X"] when matching hover col: background-color rgba(var(--grid-line-rgb), 0.5) -- a subtle column highlight
- Alternative simpler approach: JS adds .column-hover class to all slots in the hovered column. .slot.column-hover: background var(--board-bg) with slight brightness change, border-color #94a3b8. No ghost disc preview per user decision.

Win animation:
- .slot .disc.glow: box-shadow 0 0 12px 4px var(--win-glow), animation glow-pulse 1s ease-in-out infinite alternate
- @keyframes glow-pulse: from box-shadow 0 0 8px 2px rgba(22,163,74,0.4), to box-shadow 0 0 16px 6px rgba(22,163,74,0.6)
- .slot .disc.dimmed: opacity 0.4, filter grayscale(0.3)
- SVG .win-line.animate: animation draw-line 0.6s ease-out forwards
- @keyframes draw-line: to stroke-dashoffset 0

Winning line SVG:
- .win-line-overlay: position absolute, top 0, left 0, width 100%, height 100%, pointer-events none, z-index 10

Status text (.status):
- font-size 1.1rem, font-weight 600, text-align center, margin 16px 0
- Color changes with current player: .status.red-turn color var(--red), .status.yellow-turn color var(--gold)

Buttons (#reset, #undo):
- background transparent, border 1px solid var(--grid-line), border-radius 10px, padding 8px 16px
- color var(--text-muted), font-weight 500
- hover: background var(--board-bg), border-color #94a3b8
- transition all 0.15s

Game modal (.game-modal):
- backdrop: rgba(0,0,0,0.3) with backdrop-filter blur(4px)
- Modal content: white, border-radius 16px, padding 32px, box-shadow similar to game container
- Play Again button: background var(--win-glow), color white, border-radius 10px, padding 10px 24px, font-weight 600

Scores:
- Clean inline display, .red-score color var(--red), .yellow-score color var(--gold)

REMOVE: any existing @media (prefers-color-scheme: dark) block entirely. Light theme only per user decision.

All animation @keyframes inside @media (prefers-reduced-motion: no-preference).
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'),h=fs.readFileSync('projects/connect-four/index.html','utf8'),c=fs.readFileSync('projects/connect-four/style.css','utf8');var ok=h.includes('win-line-overlay')&&c.includes('disc-drop')&&c.includes('f8fafc')&&c.includes('dc2626')&&c.includes('ca8a04')&&c.includes('16a34a')&&c.includes('glow-pulse')&&c.includes('draw-line')&&!c.includes('prefers-color-scheme: dark')&&c.includes('prefers-reduced-motion');console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <done>Apple-esque light theme with specified color palette. Disc drop bounce keyframes. Glow pulse for winning discs. SVG draw-line animation. Column hover highlight. Dark mode removed. All animations respect prefers-reduced-motion.</done>
</task>

<task type="auto">
  <name>Task 2: Update GameUI for bounce drops, glow wins, column hover, and SVG line</name>
  <files>projects/connect-four/script.js</files>
  <action>
Modify GameUI class only (lines 126-380). Do NOT touch GameState class (lines 2-124).

1. updateBoard() changes:
   - When placing a new disc (slot gets a player), create the disc div with .disc class + player class (red/yellow)
   - If this is the NEWLY placed disc (the move that just happened), also add .dropping class to trigger the bounce animation. Detect the new disc by comparing with previous board state, or track the last move coordinates from handleMove.
   - After animation completes (400ms), remove .dropping class (use setTimeout or animationend event)

2. handleMove(col) changes:
   - Before calling updateBoard, store the move result's row/col as this.lastMove = { row, col }
   - After updateBoard, if result.isWin: call showWinAnimation with slight delay (500ms, after drop animation finishes) before showing modal

3. showWinAnimation(result) rewrite:
   - result contains isWin, winningCells (array of [row, col] pairs), and player
   - For each winning cell: find the slot element, add .glow class to its disc
   - For all OTHER occupied slots: add .dimmed class to their discs
   - Calculate SVG line coordinates: get the first and last winning cell positions using getBoundingClientRect relative to the board container. Set the SVG line x1,y1,x2,y2 attributes to these coordinates. Add .animate class to the line to trigger the draw-line animation.
   - Show modal after SVG line animation completes (~600ms delay)

4. Column hover (replace showDropPreview):
   - On mouseover of a slot: get the col from data-col. Remove .column-hover from all slots. Add .column-hover to all slots in that column (querySelectorAll with matching data-col).
   - On mouseout of the grid: remove .column-hover from all slots. (Keep existing mouseout handler structure.)
   - Do NOT show a ghost disc preview -- just the subtle column background highlight per user decision.

5. Reset/undo cleanup:
   - On reset() and handleUndo(): clear all animation classes (.dropping, .glow, .dimmed, .column-hover). Reset SVG line (set stroke-dashoffset back to 500, remove .animate class, reset x1/y1/x2/y2 to 0).

6. updateBoard general:
   - Remove the current innerHTML = '' clearing (line 222). Instead, clear disc children from each slot without recreating the grid. OR keep recreating but ensure .dropping is only added to the new disc.

7. Status text: add .red-turn or .yellow-turn class to .status element based on currentPlayer for colored turn indicator.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'),s=fs.readFileSync('projects/connect-four/script.js','utf8');var ok=s.includes('dropping')&&s.includes('glow')&&s.includes('dimmed')&&s.includes('column-hover')&&s.includes('win-line')&&s.includes('GameState')&&s.includes('GameUI');console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <done>Discs animate with bounce on drop. Winning discs glow green, others dim. SVG line draws through winning four. Column hover highlights on mouseover. All animation classes cleaned up on reset/undo. GameState class untouched.</done>
</task>

</tasks>

<verification>
- Open projects/connect-four/index.html in browser
- Board is light/clean: white background, light gray board, soft shadows, rounded corners
- Click a column: disc drops with bounce animation (~400ms)
- Hover over columns: subtle background highlight follows mouse
- Win a game: green line draws through winning 4 discs, discs glow, others dim
- Reset: all animations cleared, board resets
- Undo: last move removed cleanly
- In OS settings, enable prefers-reduced-motion: disc appears instantly (no bounce), win highlight appears without animation
- No dark mode override (check with system dark mode enabled -- should stay light)
- npm test passes
</verification>

<success_criteria>
- Apple-esque light theme with specified hex colors
- Physics bounce disc drop animation is visually satisfying
- Winning line SVG animation + disc glow creates a memorable win moment
- Column hover highlight provides clear feedback
- Dark mode CSS removed
- All animations respect prefers-reduced-motion
- GameState logic completely unchanged
</success_criteria>

<output>
After completion, create .planning/phases/04-per-project-polish-redesigns/04-05-SUMMARY.md
</output>
