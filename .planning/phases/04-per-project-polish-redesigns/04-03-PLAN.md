---
phase: 04-per-project-polish-redesigns
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/studybuddy/index.html
  - projects/studybuddy/styles.css
  - studybuddy/index.html
  - index.html
autonomous: true
requirements:
  - REDESIGN-01
  - REDESIGN-02

must_haves:
  truths:
    - "Page title says Study Smart, not StudyBuddy"
    - "All references to StudyBuddy are renamed to Study Smart across the codebase"
    - "The page renders as a retro OS desktop with grid-paper background and warm brown palette"
    - "Each section is in a retro window panel with title bar, minimize/maximize/close buttons"
    - "Layout has: Timer (left top), To-do list (left bottom), Music grid (right top), Volume controls (right middle), Quote widget (right bottom)"
    - "The title Study Smart appears in a cursive/script font"
  artifacts:
    - path: "projects/studybuddy/index.html"
      provides: "Complete retro OS desktop HTML structure with all window panels"
      contains: "Study Smart"
    - path: "projects/studybuddy/styles.css"
      provides: "Retro window chrome, grid-paper background, warm brown palette"
      contains: "grid-paper"
    - path: "studybuddy/index.html"
      provides: "Redirect stub updated with Study Smart name"
      contains: "Study Smart"
    - path: "index.html"
      provides: "Portfolio shell project card updated from StudyBuddy to Study Smart"
      contains: "Study Smart"
  key_links:
    - from: "projects/studybuddy/index.html"
      to: "projects/studybuddy/styles.css"
      via: "Retro window chrome classes applied to panel divs"
      pattern: "desktop-panel|window-titlebar"
    - from: "index.html"
      to: "projects/studybuddy/"
      via: "Portfolio shell links to project"
      pattern: "studybuddy"
---

<objective>
Rename StudyBuddy to Study Smart and build the retro OS desktop HTML/CSS shell with all window panels.

Purpose: Transform StudyBuddy from a generic study tool into a visually distinctive retro OS desktop. This plan creates the structural foundation (HTML layout + CSS skin); the next plan wires in functionality.
Output: Complete HTML structure with retro window panels + full CSS theme. All references renamed.
</objective>

<execution_context>
@C:/Users/adamc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adamc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-project-polish-redesigns/04-CONTEXT.md
@.planning/phases/04-per-project-polish-redesigns/04-RESEARCH.md

@projects/studybuddy/index.html
@projects/studybuddy/styles.css
@projects/studybuddy/script.js

<interfaces>
DOM selectors used by existing script.js (MUST be preserved or updated in lockstep):

IDs: startBtn, resetBtn, minutes, seconds, milliseconds, sessionCount, totalSessions, taskInput, taskDueDate, taskPriority, addTaskBtn, taskList, taskSearch, completedPomodoros, focusTime, tasksCompleted

Classes/selectors: .progress-ring-circle, .session-count, .sound-recommendation, .accountability-message, .timer-container, .mode-btn, .filter-btn, .sound-toggle-btn, .sound-options, .sound-option, .task-item, .task-checkbox, .task-text, .delete-task, section (for nav intersection), .nav-links a

QuerySelectorAll: 'a[href^="#"]', '.mode-btn', '.filter-btn', '.sound-option'

Existing CDN dependencies in index.html:
- Font Awesome 6.4.0 (keep)
- Chart.js (keep)
- Animate.css (remove -- replaced by custom CSS)
- Google Fonts currently: Poppins (will add Pacifico for cursive title)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename StudyBuddy to Study Smart everywhere and rewrite HTML to retro OS desktop</name>
  <files>projects/studybuddy/index.html, studybuddy/index.html, index.html</files>
  <action>
RENAME across codebase:
- projects/studybuddy/index.html: title tag, h1, any visible text
- studybuddy/index.html (redirect stub): update text/title
- index.html (portfolio shell): update project card title, description, any alt text
- Do NOT rename the directory (projects/studybuddy/) -- URL paths stay the same

REWRITE projects/studybuddy/index.html as a retro OS desktop:

Head section:
- Title: "Study Smart"
- Keep Font Awesome 6.4.0 CDN link
- Keep Chart.js CDN link
- REMOVE Animate.css CDN link (no longer needed)
- ADD Google Font: Pacifico (cursive title font): https://fonts.googleapis.com/css2?family=Pacifico&display=swap
- Keep Poppins Google Font for body text
- Link to styles.css

Body structure -- NO nav bar. The entire page is a desktop:

1. Desktop header: div.desktop-header containing h1 "Study Smart" in Pacifico font, centered

2. Desktop container: div.desktop containing all panels in CSS Grid layout (2 columns on desktop, 1 column on mobile):

   LEFT COLUMN:
   a. Timer Panel (div.desktop-panel#timerPanel):
      - div.window-titlebar with: div.window-buttons (span.window-btn.close, span.window-btn.minimize[data-panel="timerPanel"], span.window-btn.maximize), span.window-title "Timer"
      - div.window-body containing:
        - Mode tabs: div.timer-modes with button.mode-btn for Pomodoro, Short Break, Long Break (same classes as current)
        - Timer display: div.timer-display with span#minutes, span.timer-colon ":", span#seconds (keep IDs for JS)
        - Progress ring SVG (keep .progress-ring-circle class)
        - Session counter: span#sessionCount / span#totalSessions
        - Controls: button#startBtn "START", button#resetBtn (icon), settings gear icon button
        - span#milliseconds can be hidden or removed if not visually needed

   b. To-Do Panel (div.desktop-panel#todoPanel):
      - Window titlebar with buttons + title "To-Do List"
      - div.window-body containing:
        - Input row: input#taskInput, input#taskDueDate (type date), select#taskPriority, button#addTaskBtn
        - Search: input#taskSearch
        - Filter buttons: div with button.filter-btn for All, Active, Completed
        - Task list: ul#taskList
        - (Task items are rendered by JS using createElement -- the UL just needs to exist)

   RIGHT COLUMN:
   c. Music Panel (div.desktop-panel#musicPanel):
      - Window titlebar + title "Music"
      - div.window-body containing:
        - div.genre-grid: 10 buttons in a 5x2 CSS grid, each button.genre-btn with a Font Awesome icon + genre name. Genres: Lo-Fi (fa-headphones), Jazz (fa-music), Piano (fa-piano -- use fa-music if not available), Classical (fa-violin -- use fa-music), Acoustic (fa-guitar), Kalimba (fa-drum), K-Pop (fa-star), Cafe (fa-mug-hot), Library (fa-book), Nature (fa-leaf)
        - Each genre button: data-genre attribute with the genre name

   d. Volume Panel (div.desktop-panel#volumePanel):
      - Window titlebar + title "Now Playing"
      - div.window-body containing:
        - div.now-playing-info: genre name text (updates when genre selected)
        - div.playback-controls: speaker icon, input[type=range]#volumeSlider (min 0 max 100 value 50), play/pause button#musicPlayBtn

   e. Quote Panel (div.desktop-panel#quotePanel):
      - Window titlebar + title "Inspiration"
      - div.window-body containing:
        - div.quote-content: large quotation mark span, blockquote#quoteText, cite#quoteAuthor
        - button#refreshQuote (refresh icon)

3. Hidden sections (displayed as modal/overlay when triggered):
   - Stats section: div.desktop-panel.overlay-panel#statsPanel -- same window chrome, contains Chart.js canvas and stat displays (span#completedPomodoros, span#focusTime, span#tasksCompleted). Hidden by default, toggled by a stats button in the header or timer panel.
   - Settings section: div.desktop-panel.overlay-panel#settingsPanel -- same window chrome, contains all settings inputs. Hidden by default.

4. Keep existing script.js reference at the bottom

CRITICAL: Every ID and class that script.js uses (listed in interfaces above) MUST exist in the new HTML. If the HTML structure changes around them (e.g., .sound-options becomes .genre-grid), the script.js will be updated in Plan 04 to match. For this plan, preserve the IDs but the surrounding structure can change.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'),h=fs.readFileSync('projects/studybuddy/index.html','utf8'),r=fs.readFileSync('studybuddy/index.html','utf8'),p=fs.readFileSync('index.html','utf8');var ok=h.includes('Study Smart')&&!h.includes('animate.css')&&h.includes('Pacifico')&&h.includes('desktop-panel')&&h.includes('window-titlebar')&&h.includes('timerPanel')&&h.includes('todoPanel')&&h.includes('musicPanel')&&h.includes('quotePanel')&&h.includes('genre-grid')&&r.includes('Study Smart')&&p.includes('Study Smart');console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <done>All StudyBuddy references renamed to Study Smart. HTML rewritten as retro OS desktop with 5 main panels (Timer, To-Do, Music, Volume, Quote) plus hidden Stats and Settings overlay panels. All JS-dependent IDs preserved. Pacifico font loaded. Animate.css removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create retro OS desktop CSS theme</name>
  <files>projects/studybuddy/styles.css</files>
  <action>
REWRITE projects/studybuddy/styles.css entirely with retro OS desktop theme:

Root variables:
- --brown-dark: #3E2723 (darkest brown, text)
- --brown-medium: #5C3D2E (window chrome, borders)
- --brown-light: #8D6E63 (hover states, secondary)
- --brown-pale: #D7CCC8 (backgrounds, muted)
- --cream: #FFF8E1 (page background base)
- --accent: #FF6F00 (orange accent for buttons, active states)
- --text: #3E2723
- --text-muted: #795548

Body/page:
- background: --cream with a CSS grid-paper pattern overlay. Use repeating-linear-gradient to create subtle grid lines (#D7CCC8 at 1px thickness) at 20px intervals horizontally and vertically on top of the cream base. This creates the graph/grid paper effect.
- font-family: 'Poppins', sans-serif
- margin: 0, padding: 20px, min-height: 100vh

Desktop header (.desktop-header):
- h1: font-family 'Pacifico', cursive. Color --brown-dark. font-size 2.5rem. text-align center. margin-bottom 1.5rem.

Desktop container (.desktop):
- display: grid
- grid-template-columns: 1fr 1fr (2 equal columns)
- gap: 20px
- max-width: 1200px, margin: 0 auto

Window panel (.desktop-panel):
- background: white
- border: 2px solid --brown-medium
- border-radius: 8px
- box-shadow: 3px 3px 0px --brown-pale (retro offset shadow)
- overflow: hidden

Window titlebar (.window-titlebar):
- background: linear-gradient(to bottom, --brown-medium, --brown-dark)
- color: white
- padding: 8px 12px
- display: flex, align-items: center
- cursor: grab (for drag -- wired in Plan 04)
- user-select: none

Window buttons (.window-buttons):
- display: flex, gap: 6px, margin-right: 10px

Window button (.window-btn):
- width: 12px, height: 12px, border-radius: 50%, border: none, cursor: pointer
- .close: background #ff5f57
- .minimize: background #ffbd2e
- .maximize: background #28c840

Window title (.window-title):
- font-size: 0.85rem, font-weight: 600

Window body (.window-body):
- padding: 16px

Panel-minimized state (.desktop-panel.minimized .window-body):
- display: none (functional minimize per user decision)

Genre grid (.genre-grid):
- display: grid, grid-template-columns: repeat(5, 1fr), gap: 8px

Genre button (.genre-btn):
- background: --brown-pale
- border: 2px solid --brown-medium
- border-radius: 6px
- padding: 12px 8px
- display: flex, flex-direction: column, align-items: center, gap: 4px
- cursor: pointer
- transition: background 0.2s, transform 0.1s
- .genre-btn:hover: background --brown-light, color white
- .genre-btn.active: background --brown-medium, color white, transform scale(0.95)
- Font Awesome icon: font-size 1.2rem
- Genre name text: font-size 0.7rem

Quote content (.quote-content):
- Large decorative quotation mark: font-size 3rem, color --brown-pale, font-family serif
- blockquote: font-style italic, font-size 1.1rem, line-height 1.6, color --brown-dark
- cite: display block, margin-top 0.5rem, font-size 0.85rem, color --text-muted

Timer display (.timer-display):
- font-size: 4rem, font-weight: 700, font-family monospace, color --brown-dark, text-align center

Mode buttons (.mode-btn):
- background transparent, border 1px solid --brown-medium, border-radius 4px, padding 6px 12px
- .mode-btn.active: background --brown-medium, color white

Start button (#startBtn):
- background --accent, color white, border none, border-radius 6px, padding 12px 32px, font-size 1.1rem, font-weight 600
- hover: filter brightness(1.1)

Task input area:
- Inputs: border 1px solid --brown-pale, border-radius 4px, padding 8px, focus border-color --brown-medium

Overlay panels (.overlay-panel):
- position: fixed, top: 50%, left: 50%, transform translate(-50%,-50%), z-index: 1000, width 90%, max-width 500px
- Initially hidden (display: none), toggled by JS

Responsive (below 768px):
- .desktop: grid-template-columns: 1fr (single column stack)
- .genre-grid: grid-template-columns: repeat(3, 1fr) (3 cols on mobile)
- .timer-display: font-size 3rem
- Panels are NOT draggable on mobile (cursor default on titlebar)
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'),c=fs.readFileSync('projects/studybuddy/styles.css','utf8');var ok=c.includes('5C3D2E')&&c.includes('grid')&&c.includes('repeating-linear-gradient')&&c.includes('Pacifico')&&c.includes('window-titlebar')&&c.includes('desktop-panel')&&c.includes('genre-grid')&&c.includes('768px');console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <done>Complete retro OS CSS theme applied. Grid-paper background with cream base. Warm brown palette throughout. Window chrome with colored buttons. Genre grid is 5x2 on desktop, 3-col on mobile. Panels stack on mobile. Overlay panels for stats/settings.</done>
</task>

</tasks>

<verification>
- Open projects/studybuddy/index.html in browser -- should see retro OS desktop layout
- Title shows "Study Smart" in cursive Pacifico font
- 5 panels visible in 2-column grid layout
- Window chrome has red/yellow/green buttons
- Grid-paper background visible (cream with subtle lines)
- Brown color palette throughout
- Resize to mobile width -- panels stack vertically, genre grid becomes 3 columns
- Open index.html -- portfolio card says "Study Smart"
- NOTE: JS functionality will be broken at this point -- that is expected. Plan 04 wires the new HTML to the existing logic.
</verification>

<success_criteria>
- Zero references to "StudyBuddy" remain in visible UI text
- Retro OS desktop layout renders with all 5 panels
- Grid-paper background and warm brown palette are visually correct
- Window chrome has functional minimize button (CSS only -- JS in Plan 04)
- Mobile responsive: single column stack
</success_criteria>

<output>
After completion, create .planning/phases/04-per-project-polish-redesigns/04-03-SUMMARY.md
</output>
