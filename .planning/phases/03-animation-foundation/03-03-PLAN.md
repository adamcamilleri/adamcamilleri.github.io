---
phase: 03-animation-foundation
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - projects/handoff/index.html
  - projects/handoff/script.js
  - projects/handoff/style.css
  - projects/songdle/index.html
  - projects/songdle/script.js
  - projects/songdle/style.css
  - projects/taskmaster/src/components/TaskListSkeleton.tsx
  - projects/taskmaster/src/app/tasks/page.tsx
autonomous: true
requirements:
  - ANIM-03

must_haves:
  truths:
    - "Handoff chat generation shows a shimmer skeleton in the messages area while the AI response is loading"
    - "Songdle shows a shimmer skeleton in the game area while the daily song data is fetching"
    - "TaskMaster shows a shimmer skeleton in the task list area while tasks are loading from the API"
    - "All skeletons display for at least 400ms to prevent flash on fast loads"
    - "All skeletons crossfade to real content with a 200-300ms transition"
  artifacts:
    - path: "projects/handoff/index.html"
      provides: "Skeleton markup for chat response area, shared/animations.css link"
      contains: "skeleton"
    - path: "projects/handoff/script.js"
      provides: "showSkeleton/hideSkeleton calls around chat generation fetch"
      contains: "showSkeleton"
    - path: "projects/songdle/index.html"
      provides: "Skeleton markup for game loading state, shared/animations.css link"
      contains: "skeleton"
    - path: "projects/songdle/script.js"
      provides: "showSkeleton/hideSkeleton calls around daily song fetch"
      contains: "showSkeleton"
    - path: "projects/taskmaster/src/components/TaskListSkeleton.tsx"
      provides: "React skeleton component using Tailwind animate-pulse"
      contains: "TaskListSkeleton"
    - path: "projects/taskmaster/src/app/tasks/page.tsx"
      provides: "TaskListSkeleton rendered during loading state"
      contains: "TaskListSkeleton"
  key_links:
    - from: "projects/handoff/script.js"
      to: "shared/animations.js"
      via: "import showSkeleton/hideSkeleton"
      pattern: "import.*showSkeleton|import.*hideSkeleton"
    - from: "projects/songdle/script.js"
      to: "shared/animations.js"
      via: "import showSkeleton/hideSkeleton"
      pattern: "import.*showSkeleton|import.*hideSkeleton"
    - from: "projects/taskmaster/src/app/tasks/page.tsx"
      to: "projects/taskmaster/src/components/TaskListSkeleton.tsx"
      via: "React component import"
      pattern: "import.*TaskListSkeleton"
---

<objective>
Add skeleton loading states to Handoff (chat generation), Songdle (daily song fetch), and TaskMaster (task list load). Handoff and Songdle use the shared/animations.css skeleton classes and shared/animations.js show/hide utilities. TaskMaster uses a standalone React/Tailwind skeleton component (it has its own build system and cannot import the shared vanilla JS module).

Purpose: Async operations show content-shaped shimmer placeholders instead of blank areas, reducing perceived load time and making the portfolio feel polished.
Output: Skeleton markup and wiring in all three projects, TaskListSkeleton React component.
</objective>

<execution_context>
@C:/Users/adamc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adamc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-animation-foundation/03-CONTEXT.md
@.planning/phases/03-animation-foundation/03-RESEARCH.md
@.planning/phases/03-animation-foundation/03-01-SUMMARY.md

<interfaces>
<!-- From shared/animations.js (created in Plan 01): -->
export function showSkeleton(container: Element): number   // adds 'is-loading' class, returns timestamp
export function hideSkeleton(container: Element, startTime: number): void  // enforces 400ms min, crossfades

<!-- From shared/animations.css (created in Plan 01): -->
Classes: .skeleton, .skeleton-text, .skeleton-text--short, .skeleton-text--full,
         .skeleton-circle, .skeleton-block, .is-loading, .is-loaded
Shimmer: .skeleton::after has @keyframes shimmer (1.5s infinite translateX)
State: .is-loading shows skeleton, .is-loaded fades in real content (200ms opacity transition)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading to Handoff chat generation and Songdle daily song fetch</name>
  <files>projects/handoff/index.html, projects/handoff/script.js, projects/handoff/style.css, projects/songdle/index.html, projects/songdle/script.js, projects/songdle/style.css</files>
  <action>
**Handoff skeleton:**

1. In `projects/handoff/index.html`:
   - Add `<link rel="stylesheet" href="/shared/animations.css">` in the `<head>` after the existing style.css link.
   - Inside the `#messages` div (line 113), add a skeleton template that mirrors a chat response. Place it after the `#welcomeState` div:
     ```html
     <div class="chat-skeleton hidden" id="chatSkeleton">
       <div class="skeleton skeleton-circle" style="width:32px;height:32px;flex-shrink:0"></div>
       <div style="flex:1">
         <div class="skeleton skeleton-text skeleton-text--short"></div>
         <div class="skeleton skeleton-text skeleton-text--full"></div>
         <div class="skeleton skeleton-text skeleton-text--full"></div>
         <div class="skeleton skeleton-text skeleton-text--short"></div>
       </div>
     </div>
     ```

2. In `projects/handoff/script.js`:
   - Add an import at the top of the IIFE or use a dynamic import approach. Since this file uses an IIFE (not ES modules), add a `<script type="module">` block in the HTML instead. OR, since showSkeleton/hideSkeleton are simple, inline the logic directly:
     - At the top of the IIFE, add references: `var chatSkeleton = document.getElementById('chatSkeleton');`
     - Create local skeleton helpers that mirror the shared module behavior:
       ```javascript
       var SKELETON_MIN_MS = 400;
       function showChatSkeleton() {
         if (chatSkeleton) { chatSkeleton.classList.remove('hidden'); chatSkeleton.classList.add('is-loading'); }
         return Date.now();
       }
       function hideChatSkeleton(startTime) {
         var elapsed = Date.now() - startTime;
         var remaining = Math.max(0, SKELETON_MIN_MS - elapsed);
         setTimeout(function() {
           if (chatSkeleton) { chatSkeleton.classList.add('hidden'); chatSkeleton.classList.remove('is-loading'); }
         }, remaining);
       }
       ```
     - NOTE: Since handoff/script.js is a non-module IIFE, it cannot use ES module imports. Inline the skeleton logic. The shared/animations.css provides the visual styling (shimmer animation). This is simpler and avoids changing the script loading architecture.
   - In the chat send flow (around line 224 and line 365 where `fetch(API_BASE + '/api/chat'` is called): call `var skelStart = showChatSkeleton()` before the fetch, and `hideChatSkeleton(skelStart)` in the .then() callback before `appendMessage`.
   - In the initial generation flow (around line 592 where the onboarding completion triggers the first generation): same pattern -- show skeleton before fetch, hide after response.

3. In `projects/handoff/style.css`, add minimal layout for the skeleton:
   ```css
   .chat-skeleton {
     display: flex;
     gap: 12px;
     padding: 16px;
     align-items: flex-start;
   }
   ```

**Songdle skeleton:**

4. In `projects/songdle/index.html`:
   - Add `<link rel="stylesheet" href="/shared/animations.css">` in the `<head>` after the existing style.css link.
   - Add a skeleton overlay inside the `#app` div, right after the opening `<div id="app">`. This skeleton represents the game loading state (shown while fetchDailySong runs):
     ```html
     <div class="game-skeleton" id="gameSkeleton">
       <div class="skeleton skeleton-block" style="height:48px;margin-bottom:16px"></div>
       <div class="skeleton skeleton-block" style="height:60px;margin-bottom:12px"></div>
       <div class="skeleton skeleton-block" style="height:44px;margin-bottom:16px"></div>
       <div style="display:flex;gap:8px;justify-content:center">
         <div class="skeleton" style="width:48px;height:48px;border-radius:50%"></div>
       </div>
       <div class="skeleton skeleton-text skeleton-text--short" style="margin:16px auto 0"></div>
     </div>
     ```

5. In `projects/songdle/script.js`:
   - At the top of the IIFE, add: `var gameSkeleton = document.getElementById('gameSkeleton');`
   - Add local skeleton helpers (same inline approach as Handoff):
     ```javascript
     var SKELETON_MIN_MS = 400;
     function showGameSkeleton() {
       if (gameSkeleton) { gameSkeleton.style.display = ''; gameSkeleton.classList.add('is-loading'); }
       return Date.now();
     }
     function hideGameSkeleton(startTime) {
       var elapsed = Date.now() - startTime;
       var remaining = Math.max(0, SKELETON_MIN_MS - elapsed);
       setTimeout(function() {
         if (gameSkeleton) { gameSkeleton.style.display = 'none'; gameSkeleton.classList.remove('is-loading'); }
       }, remaining);
     }
     ```
   - In the `initGame()` function (around line 782 where `setStatus('Loading...')` is called): replace or supplement the status text with `var skelStart = showGameSkeleton()`.
   - In `fetchDailySong()` (around line 794): in the .then() callback after the song data is processed, call `hideGameSkeleton(skelStart)`. The skelStart variable needs to be accessible -- store it on the state object: `state.skelStart = showGameSkeleton()` in initGame, then `hideGameSkeleton(state.skelStart)` in fetchDailySong's .then().

6. In `projects/songdle/style.css`, add:
   ```css
   .game-skeleton {
     padding: 24px 16px;
     max-width: 400px;
     margin: 0 auto;
   }
   ```

CRITICAL: Do NOT change the script loading model (IIFE to ES module) for handoff or songdle. Keep inline skeleton helpers that match shared/animations.js behavior. The shared CSS file provides all the shimmer visual styling.
CRITICAL: Skeleton shapes must mirror actual content layout (content-shaped, per CONTEXT decision).
CRITICAL: 400ms minimum display time must be enforced (per CONTEXT decision).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const checks = [['projects/handoff/index.html', ['skeleton','chatSkeleton','shared/animations.css']], ['projects/handoff/script.js', ['showChatSkeleton','hideChatSkeleton','SKELETON_MIN_MS']], ['projects/songdle/index.html', ['skeleton','gameSkeleton','shared/animations.css']], ['projects/songdle/script.js', ['showGameSkeleton','hideGameSkeleton','SKELETON_MIN_MS']]]; let ok = true; checks.forEach(([f, terms]) => { const c = fs.readFileSync(f,'utf8'); terms.forEach(t => { if(!c.includes(t)){console.error(f+' missing: '+t); ok=false;} }); }); if(!ok) process.exit(1); console.log('All skeleton integrations present');"</automated>
  </verify>
  <done>Handoff shows shimmer skeleton in chat messages area during AI generation. Songdle shows shimmer skeleton in game area during daily song fetch. Both enforce 400ms minimum display and use shared/animations.css for shimmer styling. Content-shaped skeletons mirror actual layout.</done>
</task>

<task type="auto">
  <name>Task 2: Add TaskListSkeleton React component and wire into TaskMaster loading state</name>
  <files>projects/taskmaster/src/components/TaskListSkeleton.tsx, projects/taskmaster/src/app/tasks/page.tsx</files>
  <action>
**TaskMaster uses React/Next.js with Tailwind -- it does NOT import shared/animations.js or shared/animations.css (see RESEARCH pitfall 4).**

1. Create `projects/taskmaster/src/components/TaskListSkeleton.tsx`:
   ```tsx
   export default function TaskListSkeleton() {
     return (
       <div className="space-y-2" aria-label="Loading tasks" role="status">
         <span className="sr-only">Loading tasks...</span>
         {[...Array(5)].map((_, i) => (
           <div
             key={i}
             className="flex bg-[#111] border border-[#1a1a1a] rounded-xl overflow-hidden"
           >
             {/* Priority strip skeleton */}
             <div className="w-[3px] flex-shrink-0 bg-[#222]" />

             <div className="flex-1 px-4 py-3.5">
               <div className="flex items-center gap-3">
                 {/* Checkbox skeleton */}
                 <div className="flex-shrink-0 w-[18px] h-[18px] rounded-full bg-[#1a1a1a] animate-pulse" />
                 {/* Title skeleton - varying widths for realism */}
                 <div
                   className="flex-1 h-4 bg-[#1a1a1a] rounded animate-pulse"
                   style={{ width: `${50 + (i * 10) % 40}%` }}
                 />
                 {/* Priority badge skeleton */}
                 <div className="w-14 h-5 bg-[#1a1a1a] rounded-full animate-pulse" />
               </div>
             </div>
           </div>
         ))}
       </div>
     );
   }
   ```

   The skeleton mirrors the TaskList component layout exactly: same border-radius (rounded-xl), same padding (px-4 py-3.5), same border color (#1a1a1a), same priority strip width (3px). Uses Tailwind `animate-pulse` for shimmer effect (consistent with dark theme).

2. Update `projects/taskmaster/src/app/tasks/page.tsx`:
   - Add import at the top: `import TaskListSkeleton from '@/components/TaskListSkeleton'`
   - Replace the loading state (line 102-103):
     ```tsx
     // Before:
     <div className="text-center py-24 text-[#333] text-sm">Loading...</div>
     // After:
     <TaskListSkeleton />
     ```

Do NOT modify any other TaskMaster files. The skeleton component is self-contained and uses existing Tailwind classes.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const skel = fs.readFileSync('projects/taskmaster/src/components/TaskListSkeleton.tsx','utf8'); const page = fs.readFileSync('projects/taskmaster/src/app/tasks/page.tsx','utf8'); const checks = [[skel, ['TaskListSkeleton','animate-pulse','aria-label','Loading tasks']], [page, ['TaskListSkeleton','import']]]; let ok = true; checks.forEach(([c, terms]) => terms.forEach(t => { if(!c.includes(t)){console.error('Missing: '+t); ok=false;} })); if(!ok) process.exit(1); console.log('TaskMaster skeleton component created and wired');"</automated>
  </verify>
  <done>TaskListSkeleton.tsx exists with content-shaped skeleton matching TaskList layout. tasks/page.tsx renders TaskListSkeleton during loading state instead of "Loading..." text. Uses Tailwind animate-pulse, not shared/animations.css. Accessible with aria-label and sr-only text.</done>
</task>

</tasks>

<verification>
- Handoff: skeleton appears in chat panel during AI response generation, disappears with crossfade when response arrives
- Songdle: skeleton appears in game area during daily song fetch, disappears when song data loads
- TaskMaster: skeleton appears in task list area during initial load, disappears when tasks render
- All skeletons enforce 400ms minimum display time
- All skeleton shapes mirror actual content layout (content-shaped)
- shared/animations.css linked in Handoff and Songdle HTML
- TaskMaster uses standalone Tailwind skeleton (no shared module dependency)
- `npm test` still passes (existing tests not broken)
</verification>

<success_criteria>
- Three projects show shimmer skeleton loading states during their primary async operations
- Handoff chat generation: skeleton in messages area with circle + text bars
- Songdle daily song fetch: skeleton in game area with block placeholders
- TaskMaster task list load: skeleton matching TaskList component layout
- No skeleton flash on fast loads (400ms minimum enforced)
- Skeletons crossfade to real content (200-300ms transition via shared CSS)
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-foundation/03-03-SUMMARY.md`
</output>