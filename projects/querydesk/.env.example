# Anthropic API key for SQL generation. The app runs without it: generation
# shows a banner, everything else (catalog, manual SQL, export) works.
ANTHROPIC_API_KEY=

# Optional: override where app.db and demo_mars.db are created.
# QUERYDESK_DATA_DIR=
